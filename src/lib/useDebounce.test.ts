/**
 * Tier 3 — generic debounce hook. Use vi.useFakeTimers().
 * See tests/TEST_PLAN.md → "Tier 3: useDebounce".
 */
import { describe, it } from "vitest";
// import { renderHook } from "@testing-library/react";
// import { useDebounce } from "./useDebounce";

describe("useDebounce", () => {
  it.todo("returns the initial value immediately");
  it.todo("only updates the debounced value after the delay elapses");
  it.todo("resets the timer when the value changes within the delay window");
});

/**
 * Tier 1 — cn() class-name merge helper.
 * See tests/TEST_PLAN.md → "Tier 1: cn".
 */
import { describe, it } from "vitest";
// import { cn } from "./utils";

describe("cn", () => {
  it.todo("joins multiple class strings");
  it.todo("dedupes conflicting tailwind classes (last wins)");
  it.todo("ignores falsy values");
  it.todo("supports conditional/object class maps");
});
